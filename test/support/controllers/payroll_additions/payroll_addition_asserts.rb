module PayrollAdditionAsserts
  def payroll_addition_response_asserts
    # period = Period.find(response_data['id'])
    #
    # assert_subset response_data.keys, period_valid_keys
    # assert_equal period.start_date, Date.parse(response_data['start_date'])
    # assert_equal period.end_date, Date.parse(response_data['end_date'])
  end
end
